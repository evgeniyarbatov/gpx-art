The idea is to build a pipeline which generates sample of images. Maybe 5-10 images of each type and asks for me to rate how good the result is. Then provide a prompt for me on how to improve the image. Use local ollama with agent to modify the code until it run. Use version control to preserve changes and metadata on each image so I can go back in history. Sometimes I may want to pick a different image for the style like specifying the length of GPX track of the total area taken by the track. The system should have minimalistic design and run with Docker / Python scripts locally on MacOS. I imagine it running daily when I on the Mac. I review the images async to provide suggestions and let it run again. Sometimes I may want to iterate a few times or change the code by pasting it to Claude. I suspect the local ollama code changes will not be as versatile as Claude. 